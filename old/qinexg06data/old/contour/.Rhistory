for( i in 1:length(t)) { points ( ii[,i], G0, col="green", lty=2, type="l"); }
# Gomertz plot for publication
#library(ade4);
nat <- read.table("nat.rls.tab", header=T);
lab <- read.table("lab.rls.tab", header=T);
isolates = as.character( nat$strain );
strains  = as.character( lab$strain );
#xlim = c( 2E-4, max(nat$a) * 1.1 );
#ylim = c( 0.05, max(nat$b) * 1.5 );
xlim = c( 2E-4, 0.02 );
ylim = c( 0.05, 0.25 );
x = nat$a;y = nat$b - 0.005;
y[4]  = y[4] + 0.005 * 2;
y[14] = y[13] + 0.005 * 2;
x[14] = x[14] * 1.1;
x[13] = x[13] * 0.9;
#postscript("gompertz-space.ps")
plot( nat$b ~ nat$a ,  col="black",  xlim=xlim, ylim=ylim, xlab="I0",ylab="G", pch= 16, log='xy' );
text( x, y, isolates, cex=0.8); 
points( lab$b ~ lab$a ,  col="black", pch=1);
xx = lab$a; yy=lab$b - 0.005;
text( xx, yy, strains, cex=0.8 );
# ARLS contour lines
 predict.A <- function ( G, t ) {  G * log(0.5) / ( 1 - exp(G * t))  }
 step = 0.001; 
 G0 <- seq( 0.05, 0.5, by= step );   # i 
 t <- c( 10, 15, 20, 25, 30, 35, 40, 45, 50, 60 );
 ii  <- matrix(nrow= length(G0), ncol= length(t) );
 for (j in 1:length(t) ) {
   for ( i in 1:length(G0) ) {
     ii[i,j] =  predict.A( G0[i], t[j] );
   }
 }
for( i in 1:length(t)) { points ( ii[,i], G0, col="black", lty=2, type="l"); }
 G0 <- seq( 0.05, 0.5, by= step );   # i 
 t <- c( 10, 15, 20, 25, 30, 35, 40, 45, 50, 60 );
 iii <- ( log(0.5) * G0 ) / (1 - exp(G0 * t));
iii
G0
G t <- c( 10, 15, 20, 25, 30, 35, 40, 45, 50, 60 );
 bb = 0.055
 aa <- ( log(0.5) * bb ) / (1 - exp(bb * t));
bb
aa
 text ( aa, bb, t, cex=0.8); 
 G0 <- seq( 0.055, 0.5, by= step );   # i 
 t <- c( 10, 15, 20, 25, 30, 35, 40, 45, 50, 60 );
 bb = 0.051
 aa <- ( log(0.5) * bb ) / (1 - exp(bb * t));
 ii  <- matrix(nrow= length(G0), ncol= length(t) );
 text ( aa, bb, t, cex=0.8); 
 for (j in 1:length(t) ) {
   for ( i in 1:length(G0) ) {
     ii[i,j] =  predict.A( G0[i], t[j] );
   }
 }
for( i in 1:length(t)) { points ( ii[,i], G0, col="black", lty=2, type="l"); }
# Gomertz plot for publication
#library(ade4);
nat <- read.table("nat.rls.tab", header=T);
lab <- read.table("lab.rls.tab", header=T);
isolates = as.character( nat$strain );
strains  = as.character( lab$strain );
#xlim = c( 2E-4, max(nat$a) * 1.1 );
#ylim = c( 0.05, max(nat$b) * 1.5 );
xlim = c( 2E-4, 0.02 );
ylim = c( 0.05, 0.25 );
x = nat$a;y = nat$b - 0.005;
y[4]  = y[4] + 0.005 * 2;
y[14] = y[13] + 0.005 * 2;
x[14] = x[14] * 1.1;
x[13] = x[13] * 0.9;
#postscript("gompertz-space.ps")
plot( nat$b ~ nat$a ,  col="black",  xlim=xlim, ylim=ylim, xlab="I0",ylab="G", pch= 16, log='xy' );
text( x, y, isolates, cex=0.8); 
points( lab$b ~ lab$a ,  col="black", pch=1);
xx = lab$a; yy=lab$b - 0.005;
text( xx, yy, strains, cex=0.8 );
# ARLS contour lines
 predict.A <- function ( G, t ) {  G * log(0.5) / ( 1 - exp(G * t))  }
 step = 0.001; 
 G0 <- seq( 0.055, 0.5, by= step );   # i 
 t <- c( 10, 15, 20, 25, 30, 35, 40, 45, 50, 60 );
 bb = 0.051
 aa <- ( log(0.5) * bb ) / (1 - exp(bb * t));
 ii  <- matrix(nrow= length(G0), ncol= length(t) );
 text ( aa, bb, t, cex=0.8); 
 for (j in 1:length(t) ) {
   for ( i in 1:length(G0) ) {
     ii[i,j] =  predict.A( G0[i], t[j] );
   }
 }
for( i in 1:length(t)) { points ( ii[,i], G0, col="black", lty=2, type="l"); }
#arrows( (tb$a - tb$a.sd), tb$b, (tb$a+tb$a.sd), tb$b, lwd=0.1, angle=90,code=3,lty=8, length=0.05,col="red");
#arrows( tb$a, (tb$b-tb$b.sd), tb$a, (tb$b+tb$b.sd), lwd=0.1, angle=90,code=3,lty=8, length=0.05,col="red");
#dev.off();
 # Gomertz plot for publication
#library(ade4);
nat <- read.table("nat.rls.tab", header=T);
lab <- read.table("lab.rls.tab", header=T);
isolates = as.character( nat$strain );
strains  = as.character( lab$strain );
#xlim = c( 2E-4, max(nat$a) * 1.1 );
#ylim = c( 0.05, max(nat$b) * 1.5 );
xlim = c( 2E-4, 0.02 );
ylim = c( 0.05, 0.25 );
x = nat$a;y = nat$b - 0.005;
y[4]  = y[4] + 0.005 * 2;
y[14] = y[13] + 0.005 * 2;
x[14] = x[14] * 1.1;
x[13] = x[13] * 0.9;
#postscript("gompertz-space.ps")
plot( nat$b ~ nat$a ,  col="black",  xlim=xlim, ylim=ylim, xlab="I0",ylab="G", pch= 16, log='xy' );
text( x, y, isolates, cex=0.8); 
points( lab$b ~ lab$a ,  col="black", pch=1);
xx = lab$a; yy=lab$b - 0.005;
text( xx, yy, strains, cex=0.8 );
# ARLS contour lines
 predict.A <- function ( G, t ) {  G * log(0.5) / ( 1 - exp(G * t))  }
 step = 0.001; 
 G0 <- seq( 0.052, 0.5, by= step );   # i 
 t <- c( 10, 15, 20, 25, 30, 35, 40, 45, 50, 60 );
 bb = 0.051
 aa <- ( log(0.5) * bb ) / (1 - exp(bb * t));
 ii  <- matrix(nrow= length(G0), ncol= length(t) );
 text ( aa, bb, t, cex=0.8); 
 for (j in 1:length(t) ) {
   for ( i in 1:length(G0) ) {
     ii[i,j] =  predict.A( G0[i], t[j] );
   }
 }
for( i in 1:length(t)) { points ( ii[,i], G0, col="black", lty=2, type="l"); }
#arrows( (tb$a - tb$a.sd), tb$b, (tb$a+tb$a.sd), tb$b, lwd=0.1, angle=90,code=3,lty=8, length=0.05,col="red");
#arrows( tb$a, (tb$b-tb$b.sd), tb$a, (tb$b+tb$b.sd), lwd=0.1, angle=90,code=3,lty=8, length=0.05,col="red");
#dev.off();
 # Gomertz plot for publication
#library(ade4);
nat <- read.table("nat.rls.tab", header=T);
lab <- read.table("lab.rls.tab", header=T);
isolates = as.character( nat$strain );
strains  = as.character( lab$strain );
#xlim = c( 2E-4, max(nat$a) * 1.1 );
#ylim = c( 0.05, max(nat$b) * 1.5 );
xlim = c( 2E-4, 0.02 );
ylim = c( 0.05, 0.25 );
x = nat$a;y = nat$b - 0.005;
y[4]  = y[4] + 0.005 * 2;
y[14] = y[13] + 0.005 * 2;
x[14] = x[14] * 1.1;
x[13] = x[13] * 0.9;
#postscript("gompertz-space.ps")
plot( nat$b ~ nat$a ,  col="black",  xlim=xlim, ylim=ylim, xlab="I0",ylab="G", pch= 16, log='xy' );
text( x, y, isolates, cex=0.8); 
points( lab$b ~ lab$a ,  col="black", pch=1);
xx = lab$a; yy=lab$b - 0.005;
text( xx, yy, strains, cex=0.8 );
# ARLS contour lines
 predict.A <- function ( G, t ) {  G * log(0.5) / ( 1 - exp(G * t))  }
 step = 0.001; 
 G0 <- seq( 0.0525, 0.5, by= step );   # i 
 t <- c( 10, 15, 20, 25, 30, 35, 40, 45, 50, 60 );
 bb = 0.051
 aa <- ( log(0.5) * bb ) / (1 - exp(bb * t));
 ii  <- matrix(nrow= length(G0), ncol= length(t) );
 text ( aa, bb, t, cex=0.8); 
 for (j in 1:length(t) ) {
   for ( i in 1:length(G0) ) {
     ii[i,j] =  predict.A( G0[i], t[j] );
   }
 }
for( i in 1:length(t)) { points ( ii[,i], G0, col="black", lty=2, type="l"); }
#arrows( (tb$a - tb$a.sd), tb$b, (tb$a+tb$a.sd), tb$b, lwd=0.1, angle=90,code=3,lty=8, length=0.05,col="red");
#arrows( tb$a, (tb$b-tb$b.sd), tb$a, (tb$b+tb$b.sd), lwd=0.1, angle=90,code=3,lty=8, length=0.05,col="red");
#dev.off();
 # Gomertz plot for publication
#library(ade4);
nat <- read.table("nat.rls.tab", header=T);
lab <- read.table("lab.rls.tab", header=T);
isolates = as.character( nat$strain );
strains  = as.character( lab$strain );
#xlim = c( 2E-4, max(nat$a) * 1.1 );
#ylim = c( 0.05, max(nat$b) * 1.5 );
xlim = c( 2E-4, 0.02 );
ylim = c( 0.05, 0.25 );
x = nat$a;y = nat$b - 0.005;
y[4]  = y[4] + 0.005 * 2;
y[14] = y[13] + 0.005 * 2;
x[14] = x[14] * 1.1;
x[13] = x[13] * 0.9;
postscript("gompertz-space.060405.ps")
plot( nat$b ~ nat$a ,  col="black",  xlim=xlim, ylim=ylim, xlab="I0",ylab="G", pch= 16, log='xy' );
text( x, y, isolates, cex=0.8); 
points( lab$b ~ lab$a ,  col="black", pch=1);
xx = lab$a; yy=lab$b - 0.005;
text( xx, yy, strains, cex=0.8 );
# ARLS contour lines
 predict.A <- function ( G, t ) {  G * log(0.5) / ( 1 - exp(G * t))  }
 step = 0.001; 
 G0 <- seq( 0.0525, 0.5, by= step );   # i 
 t <- c( 10, 15, 20, 25, 30, 35, 40, 45, 50, 60 );
 bb = 0.051
 aa <- ( log(0.5) * bb ) / (1 - exp(bb * t));
 ii  <- matrix(nrow= length(G0), ncol= length(t) );
 text ( aa, bb, t, cex=0.8); 
 for (j in 1:length(t) ) {
   for ( i in 1:length(G0) ) {
     ii[i,j] =  predict.A( G0[i], t[j] );
   }
 }
for( i in 1:length(t)) { points ( ii[,i], G0, col="black", lty=2, type="l"); }
#arrows( (tb$a - tb$a.sd), tb$b, (tb$a+tb$a.sd), tb$b, lwd=0.1, angle=90,code=3,lty=8, length=0.05,col="red");
#arrows( tb$a, (tb$b-tb$b.sd), tb$a, (tb$b+tb$b.sd), lwd=0.1, angle=90,code=3,lty=8, length=0.05,col="red");
dev.off();
 # Gomertz plot for publication
#library(ade4);
nat <- read.table("nat.rls.tab", header=T);
lab <- read.table("lab.rls.tab", header=T);
isolates = as.character( nat$strain );
strains  = as.character( lab$strain );
#xlim = c( 2E-4, max(nat$a) * 1.1 );
#ylim = c( 0.05, max(nat$b) * 1.5 );
xlim = c( 2E-4, 0.02 );
ylim = c( 0.05, 0.25 );
x = nat$a;y = nat$b - 0.005;
y[4]  = y[4] + 0.005 * 2;
y[14] = y[13] + 0.005 * 2;
x[14] = x[14] * 1.1;
x[13] = x[13] * 0.9;
postscript("gompertz-space.060405.ps, width=8, height=8")
plot( nat$b ~ nat$a ,  col="black",  xlim=xlim, ylim=ylim, xlab="I0",ylab="G", pch= 16, log='xy' );
text( x, y, isolates, cex=0.8); 
points( lab$b ~ lab$a ,  col="black", pch=1);
xx = lab$a; yy=lab$b - 0.005;
text( xx, yy, strains, cex=0.8 );
# ARLS contour lines
 predict.A <- function ( G, t ) {  G * log(0.5) / ( 1 - exp(G * t))  }
 step = 0.001; 
 G0 <- seq( 0.0525, 0.5, by= step );   # i 
 t <- c( 10, 15, 20, 25, 30, 35, 40, 45, 50, 60 );
 bb = 0.051
 aa <- ( log(0.5) * bb ) / (1 - exp(bb * t));
 ii  <- matrix(nrow= length(G0), ncol= length(t) );
 text ( aa, bb, t, cex=0.8); 
 for (j in 1:length(t) ) {
   for ( i in 1:length(G0) ) {
     ii[i,j] =  predict.A( G0[i], t[j] );
   }
 }
for( i in 1:length(t)) { points ( ii[,i], G0, col="black", lty=2, type="l"); }
#arrows( (tb$a - tb$a.sd), tb$b, (tb$a+tb$a.sd), tb$b, lwd=0.1, angle=90,code=3,lty=8, length=0.05,col="red");
#arrows( tb$a, (tb$b-tb$b.sd), tb$a, (tb$b+tb$b.sd), lwd=0.1, angle=90,code=3,lty=8, length=0.05,col="red");
dev.off();
 # Gomertz plot for publication
#library(ade4);
nat <- read.table("nat.rls.tab", header=T);
lab <- read.table("lab.rls.tab", header=T);
isolates = as.character( nat$strain );
strains  = as.character( lab$strain );
#xlim = c( 2E-4, max(nat$a) * 1.1 );
#ylim = c( 0.05, max(nat$b) * 1.5 );
xlim = c( 2E-4, 0.02 );
ylim = c( 0.05, 0.25 );
x = nat$a;y = nat$b - 0.005;
y[4]  = y[4] + 0.005 * 2;
y[14] = y[13] + 0.005 * 2;
x[14] = x[14] * 1.1;
x[13] = x[13] * 0.9;
postscript("gompertz-space.060405.ps", width=8, height=8)
plot( nat$b ~ nat$a ,  col="black",  xlim=xlim, ylim=ylim, xlab="I0",ylab="G", pch= 16, log='xy' );
text( x, y, isolates, cex=0.8); 
points( lab$b ~ lab$a ,  col="black", pch=1);
xx = lab$a; yy=lab$b - 0.005;
text( xx, yy, strains, cex=0.8 );
# ARLS contour lines
 predict.A <- function ( G, t ) {  G * log(0.5) / ( 1 - exp(G * t))  }
 step = 0.001; 
 G0 <- seq( 0.0525, 0.5, by= step );   # i 
 t <- c( 10, 15, 20, 25, 30, 35, 40, 45, 50, 60 );
 bb = 0.051
 aa <- ( log(0.5) * bb ) / (1 - exp(bb * t));
 ii  <- matrix(nrow= length(G0), ncol= length(t) );
 text ( aa, bb, t, cex=0.8); 
 for (j in 1:length(t) ) {
   for ( i in 1:length(G0) ) {
     ii[i,j] =  predict.A( G0[i], t[j] );
   }
 }
for( i in 1:length(t)) { points ( ii[,i], G0, col="black", lty=2, type="l"); }
#arrows( (tb$a - tb$a.sd), tb$b, (tb$a+tb$a.sd), tb$b, lwd=0.1, angle=90,code=3,lty=8, length=0.05,col="red");
#arrows( tb$a, (tb$b-tb$b.sd), tb$a, (tb$b+tb$b.sd), lwd=0.1, angle=90,code=3,lty=8, length=0.05,col="red");
dev.off();
ls();
nat <- read.table("nat.rls.tab", header=T);
lab <- read.table("lab.rls.tab", header=T);
isolates = as.character( nat$strain );
strains  = as.character( lab$strain );
ylim = c( 3E-4, 0.02 );
xlim = c( 0.05, 0.21 );
#for text labels
y = nat$a * 1.2;x = nat$b;
y[2] = y[2] * 0.9; x[2] = x[2] + 0.007
y[9] = y[9] * 0.7
#postscript("gompertz-space.072805.ps", width=8, height=8)
plot( nat$a ~ nat$b ,  col="black",  xlim=xlim, ylim=ylim, xlab="G",ylab="R0", pch= 19, log='y' );
text( x, y, isolates, cex=0.8); 
points( lab$a ~ lab$b ,  col="black", pch=1);
yy = lab$a * 1.2; xx=lab$b;
text( xx, yy, strains, cex=0.8 );
# ARLS contour lines
 predict.A <- function ( G, t ) {  G * log(0.5) / ( 1 - exp(G * t))  }
 step = 0.001; 
 G0 <- seq( 0.0525, 0.5, by= step );   # i 
 t <- c( 10, 15, 20, 25, 30, 35, 40, 45, 50, 60 );
 bb = 0.051
 aa <- ( log(0.5) * bb ) / (1 - exp(bb * t));
 ii  <- matrix(nrow= length(G0), ncol= length(t) );
 text ( bb, aa, t, cex=0.8); 
 for (j in 1:length(t) ) {
   for ( i in 1:length(G0) ) {
     ii[i,j] =  predict.A( G0[i], t[j] );
   }
 }
for( i in 1:length(t)) { points ( G0, ii[,i], col="black", lty=2, type="l"); }
#arrows( (tb$a - tb$a.sd), tb$b, (tb$a+tb$a.sd), tb$b, lwd=0.1, angle=90,code=3,lty=8, length=0.05,col="red");
#arrows( tb$a, (tb$b-tb$b.sd), tb$a, (tb$b+tb$b.sd), lwd=0.1, angle=90,code=3,lty=8, length=0.05,col="red");
help(axis);
m = ( ln(nat$a) ~ b);
summary(m)
m
m = lm( ln(nat$a) ~ b);
m = lm( ln(nat$a) ~ nat$b);
nat
m = lm( log(nat$a) ~ nat$b);
m
summary(m);
nat <- read.table("nat.rls.tab", header=T);
lab <- read.table("lab.rls.tab", header=T);
isolates = as.character( nat$strain );
strains  = as.character( lab$strain );
ylim = c( 3E-4, 0.02 );
xlim = c( 0.05, 0.21 );
#for text labels
y = nat$a * 1.2;x = nat$b;
y[2] = y[2] * 0.9; x[2] = x[2] + 0.007
y[9] = y[9] * 0.7
# plot( nat$a ~ nat$b ,  col="black",  xlim=xlim, ylim=ylim, xlab="G",ylab="R0", pch= 19, log='y' );
plot( nat$a ~ nat$b ,  col="black",  xlim=xlim, ylim=ylim, xlab="G",ylab="R0", pch= 19 );
text( x, y, isolates, cex=0.8); 
plot( nat$a ~ nat$b ,  col="black",  xlim=xlim, ylim=ylim, xlab="G",ylab="R0", pch= 19 );
text( x, y, isolates, cex=0.8); 
m = lm( log(nat$a) ~ nat$b);
summary(m);
lines(m, col="red");
# plot( nat$a ~ nat$b ,  col="black",  xlim=xlim, ylim=ylim, xlab="G",ylab="R0", pch= 19, log='y' );
plot( log(nat$a) ~ nat$b ,  col="black",  xlim=xlim, ylim=ylim, xlab="G",ylab="R0", pch= 19 );
text( x, y, isolates, cex=0.8); 
m = lm( log(nat$a) ~ nat$b);
summary(m);
lines(m, col="red");
plot(m)
m
plot( log(nat$a) ~ nat$b);
m = lm( log(nat$a) ~ nat$b);
summary(m);
lines(m, col="red");
plot( log(nat$a) ~ nat$b);
m = lm( log(nat$a) ~ nat$b);
lines(m, col="red");
abline(m);
help(abline);
# plot( nat$a ~ nat$b ,  col="black",  xlim=xlim, ylim=ylim, xlab="G",ylab="R0", pch= 19, log='y' );
plot( log(nat$a) ~ nat$b ,  col="black",  xlim=xlim, ylim=ylim, xlab="G",ylab="R0", pch= 19 );
text( x, y, isolates, cex=0.8 ); 
#plot( log(nat$a) ~ nat$b);
m = lm( log(nat$a) ~ nat$b);
abline(m, col="red");
#for text labels
y = log(nat$a) * 1.2;x = nat$b;
y[2] = y[2] * 0.9; x[2] = x[2] + 0.007
y[9] = y[9] * 0.7
plot( log(nat$a) ~ nat$b ,  col="black",  xlim=xlim, ylim=ylim, xlab="G",ylab="R0", pch= 19 );
text( x, y, isolates, cex=0.8 ); 
#plot( log(nat$a) ~ nat$b);
m = lm( log(nat$a) ~ nat$b);
abline(m, col="red");
plot( log(nat$a) ~ nat$b ,  col="black",  xlab="G",ylab="R0", pch= 19 );
text( x, y, isolates, cex=0.8 ); 
#plot( log(nat$a) ~ nat$b);
m = lm( log(nat$a) ~ nat$b);
abline(m, col="red");
#for text labels
y = log(nat$a) * 1.2;x = nat$b;
plot( log(nat$a) ~ nat$b ,  col="black",  xlab="G",ylab="R0", pch= 19 );
text( x, y, isolates, cex=0.8 ); 
#plot( log(nat$a) ~ nat$b);
m = lm( log(nat$a) ~ nat$b);
abline(m, col="red");
#for text labels
y = log(nat$a) * 1.1;x = nat$b;
plot( log(nat$a) ~ nat$b ,  col="black",  xlab="G",ylab="R0", pch= 19 );
text( x, y, isolates, cex=0.8 ); 
#plot( log(nat$a) ~ nat$b);
m = lm( log(nat$a) ~ nat$b);
abline(m, col="red");
#for text labels
y = log(nat$a) * 1.05;x = nat$b;
plot( log(nat$a) ~ nat$b ,  col="black",  xlab="G",ylab="R0", pch= 19 );
text( x, y, isolates, cex=0.8 ); 
#plot( log(nat$a) ~ nat$b);
m = lm( log(nat$a) ~ nat$b);
abline(m, col="red");
#for text labels
y = log(nat$a) - 0.1;x = nat$b;
plot( log(nat$a) ~ nat$b ,  col="black",  xlab="G",ylab="R0", pch= 19 );
text( x, y, isolates, cex=0.8 ); 
#plot( log(nat$a) ~ nat$b);
m = lm( log(nat$a) ~ nat$b);
abline(m, col="red");
ylim = ( min(log(nat$a)) - 1, max(log(nat$a)) + 1 )
#for text labels
y = log(nat$a) - 0.1;x = nat$b;
plot( log(nat$a) ~ nat$b ,  col="black",  xlab="G",ylab="R0", pch= 19, ylim= ylim );
text( x, y, isolates, cex=0.8 ); 
#plot( log(nat$a) ~ nat$b);
m = lm( log(nat$a) ~ nat$b);
abline(m, col="red");
min(log(nat$a))
max(log(nat$a)) + 1 
strains  = as.character( lab$strain );
ylim = ( (min(log(nat$a)) - 1), (max(log(nat$a)) + 1) )
#for text labels
y = log(nat$a) - 0.1;x = nat$b;
plot( log(nat$a) ~ nat$b ,  col="black",  xlab="G",ylab="R0", pch= 19, ylim= ylim );
text( x, y, isolates, cex=0.8 ); 
#plot( log(nat$a) ~ nat$b);
m = lm( log(nat$a) ~ nat$b);
abline(m, col="red");
ylim = c( (min(log(nat$a)) - 1), (max(log(nat$a)) + 1) )
#for text labels
y = log(nat$a) - 0.1;x = nat$b;
plot( log(nat$a) ~ nat$b ,  col="black",  xlab="G",ylab="R0", pch= 19, ylim= ylim );
text( x, y, isolates, cex=0.8 ); 
#plot( log(nat$a) ~ nat$b);
m = lm( log(nat$a) ~ nat$b);
abline(m, col="red");
ylim = c( (min(log(nat$a)) - 0.5 ), (max(log(nat$a)) + 0.5 ) )
#for text labels
y = log(nat$a) - 0.1;x = nat$b;
plot( log(nat$a) ~ nat$b ,  col="black",  xlab="G",ylab="R0", pch= 19, ylim= ylim );
text( x, y, isolates, cex=0.8 ); 
#plot( log(nat$a) ~ nat$b);
m = lm( log(nat$a) ~ nat$b);
abline(m, col="red");
ylim = c( (min(log(nat$a)) - 0.5 ), (max(log(nat$a)) + 0.5 ) )
xlim = c( min(nat$b)-0.05, max(nat$b)+0.1 )
#for text labels
y = log(nat$a) - 0.1;x = nat$b;
plot( log(nat$a) ~ nat$b ,  col="black",  xlab="G",ylab="R0", pch= 19, ylim= ylim, xlim=xlim );
text( x, y, isolates, cex=0.8 ); 
#plot( log(nat$a) ~ nat$b);
m = lm( log(nat$a) ~ nat$b);
abline(m, col="red");
ylim = c( (min(log(nat$a)) - 0.5 ), (max(log(nat$a)) + 0.5 ) )
xlim = c( min(nat$b)-0.05, max(nat$b)+0.05 )
#for text labels
y = log(nat$a) - 0.1;x = nat$b;
plot( log(nat$a) ~ nat$b ,  col="black",  xlab="G",ylab="R0", pch= 19, ylim= ylim, xlim=xlim );
text( x, y, isolates, cex=0.8 ); 
#plot( log(nat$a) ~ nat$b);
m = lm( log(nat$a) ~ nat$b);
abline(m, col="red");
ylim = c( (min(log(nat$a)) - 0.5 ), (max(log(nat$a)) + 0.5 ) )
xlim = c( min(nat$b)*0.9, max(nat$b)+0.05 )
#for text labels
y = log(nat$a) - 0.1;x = nat$b;
plot( log(nat$a) ~ nat$b ,  col="black",  xlab="G",ylab="R0", pch= 19, ylim= ylim, xlim=xlim );
text( x, y, isolates, cex=0.8 ); 
#plot( log(nat$a) ~ nat$b);
m = lm( log(nat$a) ~ nat$b);
abline(m, col="red");
ylim = c( (min(log(nat$a)) - 0.5 ), (max(log(nat$a)) + 0.5 ) )
xlim = c( min(nat$b)*0.9, max(nat$b)*1.05 )
#for text labels
y = log(nat$a) - 0.1;x = nat$b;
plot( log(nat$a) ~ nat$b ,  col="black",  xlab="G",ylab="R0", pch= 19, ylim= ylim, xlim=xlim );
text( x, y, isolates, cex=0.8 ); 
#plot( log(nat$a) ~ nat$b);
m = lm( log(nat$a) ~ nat$b);
abline(m, col="red");
summary(m);
text( 0.12, -5, "R^2=0.37 p=0.02" );
ylim = c( (min(log(nat$a)) - 0.5 ), (max(log(nat$a)) + 0.5 ) )
xlim = c( min(nat$b)*0.9, max(nat$b)*1.05 )
#for text labels
y = log(nat$a) - 0.1;x = nat$b;
plot( log(nat$a) ~ nat$b ,  col="black",  xlab="G",ylab="lnR0", pch= 19, ylim= ylim, xlim=xlim );
text( x, y, isolates, cex=0.8 ); 
#plot( log(nat$a) ~ nat$b);
m = lm( log(nat$a) ~ nat$b);
abline(m, col="red");
text( 0.14, -5.2, "R^2=0.37 p=0.02" );
strains  = as.character( lab$strain );
ylim = c( (min(log(nat$a)) - 0.5 ), (max(log(nat$a)) + 0.5 ) )
xlim = c( min(nat$b)*0.9, max(nat$b)*1.05 )
#for text labels
y = log(nat$a) - 0.1;x = nat$b;
plot( log(nat$a) ~ nat$b ,  col="black",  xlab="G",ylab="lnR0", pch= 19, ylim= ylim, xlim=xlim );
text( x, y, isolates, cex=0.8 ); 
#plot( log(nat$a) ~ nat$b);
m = lm( log(nat$a) ~ nat$b);
abline(m, col="red");
text( 0.14, -4.5, "R^2=0.37 p=0.02" );
