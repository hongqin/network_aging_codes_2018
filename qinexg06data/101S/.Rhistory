tb = read.table( "rls.tab", header=F );
avg.rls = mean( tb, na.rm=T );
sd.rls = sd( tb, na.rm = T);
tb
tb = read.table( "rls.tab", header=T );
avg.rls = mean( tb, na.rm=T );
sd.rls = sd( tb, na.rm = T);
tb
sd.rls = sd( tb, na.rm = T);
sd.rls
mean.rls
rls.mean
ls();
avg.rls
x
round( x, 5);
x = round( x, 5);
x
 tb <- read.table( "wm.out.tab", header=T);
 x = cbind( mean(tb$a), sd(tb$a), mean(tb$b), sd(tb$b));
 x = round( x, 7);
x
boot
 boot <- read.table( "boot.tab", header=F);
str(boot)
mean(boot);
mean(y)
y = mean(boot);
mean(y)
sd(y)
 tb <- read.table( "wm.out.tab", header=T);
 x = cbind( mean(tb$a), sd(tb$a), mean(tb$b), sd(tb$b));
 boot <- read.table( "boot.tab", header=F);
 rls = mean( boot );
 x = c( mean(rls), sd(rls), x );
 
 x = round( x, 7);
 out = data.frame( x );
 names( out ) = cbind("rls", "rls.sd", "a", "a.sd", "b", "b.sd");
 out;
 tb <- read.table( "wm.out.tab", header=T);
 x = cbind( mean(tb$a), sd(tb$a), mean(tb$b), sd(tb$b));
 boot <- read.table( "boot.tab", header=F);
 rls = mean( boot );
 x = cbind( mean(rls), sd(rls), x );
 
 x = round( x, 7);
 out = data.frame( x );
 names( out ) = cbind("rls", "rls.sd", "a", "a.sd", "b", "b.sd");
 out;
 write.table( out, "summary.wm.out.tab", row.names=F, col.names=T, quote=F, sep="\t");
